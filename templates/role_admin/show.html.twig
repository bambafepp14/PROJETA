{% extends 'base.html.twig' %}

{% block title %}Role{% endblock %}

{% block body %}
    <h1>Role</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ role.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ role.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_role_admin_index') }}">back to list</a>

    <a href="{{ path('app_role_admin_edit', {'id': role.id}) }}">edit</a>

    {{ include('role_admin/_delete_form.html.twig') }}
{% endblock %}
